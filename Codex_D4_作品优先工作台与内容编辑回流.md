# Codex D4：作品优先工作台与内容编辑回流

## 0. 依据与范围

用户报告 D3 已提交为 `8a0fe1d`，尚未推送。当前交接依据为用户上传的 `D3_PROJECT_DEMO_AGENT.md`，不是本轮独立源码审计、原始执行轨迹复查或测试重跑。执行时以本地工作区为准，先检查适用项目指令、HEAD、git status 和已有改动，不切回旧审阅提交。

交付说明记录：对象／组件架构上，真实模型完成钥匙门、场景冲突后重读、顺序机关源码修复和同任务追加目标；ECS 完成确定性回归与真实工程构建。新玩法的浏览器按键检查由独立真实浏览器验收执行，并非内置观察器自动理解任意玩法。不要将这些覆盖范围扩大，也不要重复将固定夹具包装为新的真实模型测试。

本轮目标：把 D1–D3 已有能力变成普通用户可在工作台连续使用的路径。不是再造 Agent，不补全部素材供应商，不重做整站布局。

## 1. 用户交付目标

用户在主工作台中可以：

对齐 idea → 确认初版方向和技术方案 → 一次发起制作 → 查看真实进度 → 打开 Demo → 找到实际内容 → 修改源资产／实例／已有行为参数，或让 Agent 改相关源码 → 更新同一 Demo → 重开后继续。

不要求完整策划和四卡成为初版前置条件。初版方向尽量利用对话已有信息，展示可修改的简短摘要；不要要求用户把聊天内容再抄一遍到长表单。未知且会影响结果的重要选项仍需明确，不伪造用户确认。

## 2. 主入口与任务模式

复用现有 ShellWorkbench、PlanningJourney、AgentTaskWorkbench、资产/场景编辑器和运行记录，先定位实际接线，不基于旧文件行号盲改。

默认“根据 idea 制作”入口明确调用 D3 的 `project-demo-agent`。D2 的零模型固定夹具保留为明确标识的示例／测试／兼容入口，不能成为真实制作失败后的静默替代。

没有可玩候选时，以主对话、初版方向、制作进度为重心；有候选后，让试玩与“继续修改”成为明显入口。旧策划版本和四卡视图保留高级访问，不迁移或清除旧项目。

沿用现有预览隔离方式。若当前只支持独立页试玩，清楚呈现项目与版本即可；不要为了新布局直接把生成脚本注入工作台权限域，也不把跨域嵌入改造设为本轮前置。

## 3. 内容导航是事实投影，不是第二套数据源

优先完成内容列表选择 → 对应编辑器，不把 3D 视口点选作为必需前置。

列表从已登记的资产、场景实例、行为关联和任务实际变更中读取，至少涵盖本轮演示项目中存在的：

- 可编辑资产和版本、被哪些实例使用；
- 场景实例和源资产引用；
- 已有 KeyDoor 的参数入口；
- 普通行为源码的 Agent／源码入口。

不能硬编码“总有两扇门”，不能根据文件名猜测所有语义关系，也不要把每个文件都生成一张内容卡。没有可靠关联的源码可以显示项目源码入口，不伪造精确组件绑定。

每个入口注明真实编辑方式：配方、实例属性、已实现参数、源码／Agent，或暂不支持。

共享门配方与某一扇门的实例属性要分开；影响列表来自当前引用，不能靠模型猜测。明确的已有权限范围内修改直接完成；真正扩大影响范围时说明差异，不逐微动作重复审批。

复杂行为没有通用参数编辑器时，提供准确源码入口即可。本轮不建设通用行为描述语言、动态表单平台或万能资产解析器。

## 4. 人工编辑和 Agent 请求携带同一个目标

从内容列表或编辑器发起“让 Agent 修改”时，沿现有接口携带必要上下文，例如：

- project_id、workspace_id；
- 目标种类、资产／实例／行为的真实引用；
- 当前编辑的源版本，以及用户正在看的候选／构建；
- 用户要求和已有任务关联。

这是语义要求，不要求新增一份统一巨型协议。优先复用已有字段与校验。

服务端重新解析目标、项目归属和可用权限；UI选中不等于授权，客户端路径也不能成为写入依据。Agent在写入前读取最新源状态，版本冲突按已有机制处理。

卡片与工作区切换后，不把上一项目的目标继续带入下一次请求。用户看旧候选时，不能用旧画面参数默默覆盖较新的编辑源；明确显示版本差异，并基于最新源实施或拒绝冲突。

人工属性保存与 Agent 工具使用同一领域服务；不得另建前端“已保存真相”，不得修改派生运行配置来规避源模型。

## 5. 源、候选和用户正在玩的会话分开显示

继续使用 D2 的状态：源已保存、正在构建、试玩已更新、更新失败，并明确其所指对象。

显示至少：当前编辑源是否有未运行改动、最新成功候选、实际打开的试玩版本。不能因为新候选成功，仍显示旧页却告诉用户眼前已更新。

新候选成功后，可以按现有采用规则更新可玩指针；用户正在游戏中时，不无提示地刷新其页面、清空进度或切到新构建。显示“新版已就绪，重新试玩”或等价操作。此处不改变既有候选发布语义，只区分指针与实际会话。

源修改失败、构建失败、模型受阻分别显示。旧可玩候选继续存在，失败不触发整项目重建或静默回到夹具。

普通玩家位置、门开关、收集和得分是运行态；停止试玩不自动回写源场景。

## 6. 继续制作与有限授权

默认继续已有项目和工作区。复用 D3 的追加目标、请求幂等、候选和累计预算，不新建一组平行逻辑。

文档中的顺序机关链使用了 26/28 次模型请求，说明本次链尾剩余预算有限，但不是所有项目的平均成本或成功率。UI应准确显示剩余范围和失败原因，不能新增任务偷偷归零预算。

纯粹切换列表、读取参数、打开已有试玩、查看日志等确定性操作，不为显示内容多调用模型。缓存可用于展示，但写入前仍需读取／校验当前版本。

授权过期或预算不足时，保留当前源、候选和用户输入。按产品明确规则提出新的有限继续请求；不能延长旧授权或仅让用户从头生成。只读浏览和已有试玩是否继续，沿其已有授权边界，不随意新增封锁或开放权限。

## 7. 最小验收矩阵

所有测试先在隔离数据和测试项目中进行。各项区分模拟、真实服务、真实浏览器和真实模型，不混加数量。

| 场景 | 预期 |
|---|---|
| 实际 Shell 发起初版 | 主入口调用 project-demo-agent，不要求先批准四张计划卡，不静默跑 D2 夹具 |
| 生成与恢复 | 显示真实任务进度，刷新／重开找到同项目与候选，不把HTTP200当作游戏已完成 |
| 共享配方修改 | 打开真实配方，显示影响实例；改尺寸后源版本、运行几何和碰撞按现有机制一致更新 |
| 单实例修改 | 只修改指定实例或其 KeyDoor 参数，另一共享外观实例不被误改 |
| 普通源码行为 | 可定位实际编辑来源；不存在通用面板时诚实显示 Agent／源码方式；物化不覆盖普通源码 |
| Agent 续改 | 选中实际目标，发送自然语言要求；任务得到正确上下文并在同工作区继续 |
| 源与旧预览差异 | 旧试玩未刷新时显示旧版本，新候选就绪后可明确切换，不丢当前玩家会话 |
| 构建失败 | 新失败候选不替换旧成功结果，用户能看具体失败并继续修改 |
| 并发与版本冲突 | 不用旧参数覆盖新源，晚到的旧结果不抢占新候选 |
| 跨项目/过期目标 | 服务端拒绝错误归属，UI不遗留上一项目的写入上下文 |
| 预算／授权耗尽 | 不自动续期、不静默切供应商、不从头重生成；成果和未发送输入保留 |
| 旧项目与布局 | 旧卡片项目仍能打开；中英文长名称、窄面板、焦点和错误提示做必要范围浏览器检查 |

至少跑一次真正的 Shell 页面用户路径，而不只挂载独立组件：打开项目 → 试玩 → 内容列表 → 编辑器 → 保存 → 更新 → 查看新结果 → 重开。

UI主要验证可以复用D3生成的测试工程，不必每次重新花费模型调用。与Agent真实续改的最后一段，在已有有效授权下独立验证；无授权时明确未验证，不以脚本化动作替代模型。

保持两种架构的相关回归；ECS真实模型补验可另行记录，不假称已有覆盖，也不要求它成为所有D4界面工作的前置。

## 8. 对 D3 已发现问题的保留

CodeBuddy 终态解析修复、结构化校验、UI状态所有权和持续状态检查沿用现有代码与回归。D4不放松JSON校验来掩盖传输问题，不让游戏循环与面板继续争写同一状态。

交付说明中100 ms后的提示保持，是一个具体任务的观察结果，不扩大成所有UI时序都已验证。

D3已有失败与基线对照记录不删除。以同环境同命令比较本轮新增问题；不把全仓历史错误清零作为本轮门槛，也不只按报错是否在修改文件里判断责任。

## 9. 实施与交付

可以分为两个有用增量：

A. 源内容投影、入口定位、选中上下文和正确任务路由。
B. 状态／候选呈现、编辑回流、真实Shell浏览器验收和必要错误恢复。

每个增量都应连到实际页面，不能只交若干不使用的类型和组件。不另建设计系统、不增加付费供应商、不把工作区变成按内容卡复制的目录，不默认合并分支或发布。

交付说明列出：用户路径、真实可编辑内容类型、未支持入口、修改范围、执行的命令与证据、模型是否真实参与、已知限制与提交SHA。

按用户现有要求提交本轮相关产品代码、测试和文档，保留已有无关未提交和已暂存修改；不自动推送。不将产品源码提交约定扩大为用户游戏分支的自动提交、合并或发布。

---

本文件为根据D3交付说明整理的D4任务建议，未实施。依据：本轮上传的 `D3_PROJECT_DEMO_AGENT.md`（实现边界、真实模型与工具结果、Limitations）。未在本轮读取 `.local/d3-evidence` 原始证据，也未核实 `8a0fe1d` 源码或远端状态。
