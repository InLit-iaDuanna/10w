# Codex 下一批：Blender 原生资产编辑往返

请继续当前本地SceneOps，在D4已完成的作品导航与编辑回流上，实现一条真实Blender资产往返。直接交付可操作代码、测试和结果，不只交计划，也不要展开全产品架构改造。

## 1. 基线与范围

先阅读当前适用项目指令、D4_ARTWORK_WORKBENCH.md，检查HEAD、git status与暂存区。D4产品提交为514648b，验证/说明为1e90f54；以实际本地为准，不切回旧远端190f43b，不撤销或混入已有无关修改。

D4报告不是外部工具已通过的证据。只读核对当前Blender适配器、资产源模型、场景实例、内容导航、物化器和构建入口。已有的BlenderAgentSession、类型化桥接、认证、隔离和导出优先复用；不同时安装另一套MCP或创建新的Agent运行框架。

本轮只接Blender→当前Web Demo。Unity、Xcode只可做明确需要的只读接口/环境调查，不开始工程迁移、不安装SDK、不同时接入多个外部服务。

## 2. 用户目标

在现有Demo中选一份共享门资产，通过真实Blender修改门框/门扇的可见几何或基础材质，保存新源版本并导出实际GLB；更新Demo后外观改变，但实例身份、KeyDoor行为及未涉及参数仍保留。随后可以再次打开同一源继续编辑，而不是每次重新生成固定立方体。

门只是验收样例，不得把整个Blender集成硬编码为一个固定演示函数。

## 3. 环境与执行

- 使用已配置Blender，读取实际路径/版本和导出能力。未安装、不兼容、缺授权、缺导出器、会话忙分别返回原因，不自动安装/升级。
- 执行位置来自登记的project/workspace/asset与本次候选，模型不能决定任意路径、端口或环境。
- 使用原有安全传输和隔离。若当前执行器明确要求系统隔离，不能通过删检查、假报sandbox能力或开启完全权限绕过。
- Blender操作按实际API线程约束执行。只停止本应用拥有的会话/任务，不误关用户其他Blender窗口。
- 模型现有工具不够时，补本轮需要的类型化操作；必须开放脚本时，明确脚本权限与真实执行边界，不能认为Python文本过滤或worktree就是沙箱。
- 取消、超时、异常、已完成和结果未知分别记录。先回读已知结果，不盲目重试写入；未知状态不能靠续授绕过。

## 4. 真实编辑源

- DoorRecipe转为Blender编辑源时创建明确的新源候选或版本，并保留原配方版本和来源；不能静默覆盖。
- 新.blend必须实际存在且可重开。人工/Agent编辑后的自由几何，不得在下一次物化时被旧配方重建覆盖。
- 原配方控件只继续暴露有真实映射的参数；不能对任意自由几何假装支持参数往返。
- 复用现有资产身份与版本语义。没有文件就不填虚构路径；只需要GLB的Web链，不强制生成无用途FBX。
- 门框、门扇与铰链保持必要结构和语义关联。源内节点和场景实例分别识别，不能全靠显示名称。
- 源.blend与运行GLB分别保存，导出是派生过程，不把GLB当作完整建模历史。

## 5. 导出、登记与游戏回流

1. 从准确的当前资产版本开始编辑，避免覆盖已更新源。
2. 导出到本次操作staging位置，真实检查格式、引用、尺寸、节点/材质和可读取性。
3. 版本登记与场景引用更新复用现有公共服务，明确共享影响范围；冲突保留源成果并恢复剩余应用，不重复造版本。
4. 当前运行物化器/加载器支持本批文件资产，而不只DoorRecipe。仅导出一个GLB、却没放进游戏不算完成。
5. 记录并处理单位、上轴、前向、枢轴和必要碰撞代理。用非对称模型和偏置铰链检查，不只用正方体。
6. 建立新候选并沿现有UI显式打开。失败保留旧可玩版，用户旧试玩页不刷新。
7. 资产外观修改不能重建KeyDoor/实例ID或改变交互距离、钥匙和角度。几何变化与碰撞需要对应处理，门框不能随门扇一起转。

## 6. 人工与Agent入口

D4实际内容列表显示真实源类型和“用Blender编辑”/“让Agent修改”等适用入口。用户手工保存后可以显式同步导出，不把任意文件watcher事件当作完成或授权。

同一.blend编辑候选不允许人工与Agent并发覆盖。检测到新源版本或其他编辑时明确冲突，保留双方内容，不假装二进制可自动合并。

增加或适配一份Blender Technical Artist技能，按当前工具加载；保留现有Director、Debug和QA。技能需进入实际Provider请求，但不得把技能声明当成工具已实现。截图只有真正送入支持图像的模型时，才可以声称模型看过。

## 7. 验收

先跑局部协议/领域测试，再独立执行真实工具和模型路径，分别报告：

A. 真实Blender打开/生成、编辑、保存、重开与GLB导出；同一个源第二次编辑仍保留第一轮变化。
B. 一次通过产品内真实Agent的修改；一次手动Blender保存回流。若某条缺环境或有效授权，明确未验证，不能用夹具替代。
C. 两个共享实例按请求范围升级；实例KeyDoor与变换保持；运行中外观、尺寸、开门铰链及碰撞实际正确。
D. 原Web流程和对象/组件、ECS两种加载/构建回归；真实模型覆盖哪种就只报告哪种。
E. 坏导出、版本冲突、重复请求、进程中断、取消、缺工具、构建失败；旧源和旧可玩候选仍可找回。
F. 重开SceneOps恢复资产源、版本及游戏候选；旧用户Blender会话不被误关。
G. 一条真实Shell：选内容→启动编辑/Agent修改→保存源→更新Demo→显式试玩。保留截图、源版本和运行证据，证据不混入产品源码。

程序化夹具通过不等于真实Blender，真实Blender成功不等于模型自主制作，画布非空不等于开门行为正确。不能在结尾写成“所有3D内容均可生成”。

## 8. 不扩张的边界

不做角色自动重拓扑/完整骨骼动画，不接付费素材服务，不迁移Unity，不做iOS发布，不重写全站布局。不新增全域哈希、冻结门禁、全量工作流系统；已有校验与安全约束不因本批改变而撤掉。

原有浏览器/依赖问题若影响本批验收，局部修正并记录；不要求全仓错误清零，不把受阻测试计为通过。真实模型旧授权过期时不自动延长；有效授权在原范围内继续使用。

## 9. 交付与提交

可分两个审阅提交：
- Blender源类型、适配器与现有内容/运行回流；
- 相关测试、真实往返证据索引与交付说明。

说明具体工具版本、执行范围、模型动作、真实文件/版本、哪些架构和哪些浏览器行为验证、剩余限制。

按用户约定提交本轮产品差异，不混入已有无关暂存/未提交内容，不自动推送。不自动提交用户游戏全部源码、不合并游戏分支、不发布。
